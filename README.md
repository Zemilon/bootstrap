# bootstrap

***run:***
```
mv ~/.bash_profile ~/bash_profile.old; curl -s \
https://raw.githubusercontent.com/Zemilon/bootstrap/master/.bash_profile \
--output ~/.bash_profile && source ~/.bash_profile && tput setaf 2; echo \
"Finished successfully"
```
